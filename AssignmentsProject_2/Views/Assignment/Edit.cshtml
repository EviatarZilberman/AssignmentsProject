@model AssignmentsProject_2.Models.Assignment

@{
    ViewData["Title"] = "Edit Assignment";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="center" style="margin-top: 50px; margin-bottom: 50px;">

@if (Model != null)
{
    <div class="center-in">
        <h1 class="text-center">@ViewData["Title"]</h1>

        <div class="text-danger" asp-validation-summary="All"></div>
    </div>
}
</div>

@*<div class="row">
*@
<div class="center">
    @*<div class="col-md-4">*@
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
       @*     <div class="form-group">
                <label asp-for="Started" class="control-label"></label>
                <input asp-for="Started" class="form-control" />
                <span asp-validation-for="Started" class="text-danger"></span>
            </div>*@
       @*     <div class="form-group">
                <label asp-for="Finish" class="control-label"></label>
                <input asp-for="Finish" class="form-control" />
                <span asp-validation-for="Finish" class="text-danger"></span>
            </div>*@
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" type="checkbox" asp-for="IsOpen" /> @Html.DisplayNameFor(model => model.IsOpen)
                @if (Model.IsOpen)
                    {
                        Model.InProgress = false;
                        Model.IsCompleted = false;
                    }
                </label>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" type="checkbox" asp-for="InProgress" /> @Html.DisplayNameFor(model => model.InProgress)
                    @if (Model.InProgress)
                    {
                        Model.IsOpen = false;
                        Model.IsCompleted = false;
                    }
                </label>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" type="checkbox" asp-for="IsCompleted" /> @Html.DisplayNameFor(model => model.IsCompleted)
                    @if (Model.IsCompleted)
                    {
                        Model.IsOpen = false;
                        Model.InProgress = false;
                    }
                </label>
            </div>
        <div class="form-group" style="text-align: center; margin-top: 50px;">
            <input type="submit" value="Save" class="btn btn-info" id="btn1" asp-action="EditAssignment" asp-controller="Assignment" />
            </div>
        </form>
    </div>
@*</div>*@
@*</div>*@



